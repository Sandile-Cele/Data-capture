@model IEnumerable<Data_capture.Models.EntryCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EcName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EcName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EcId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EcId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EcId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
